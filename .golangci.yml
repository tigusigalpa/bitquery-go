version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - misspell
  exclusions:
    presets:
      - comments
      - std-error-handling
    paths:
      - _test.go

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
